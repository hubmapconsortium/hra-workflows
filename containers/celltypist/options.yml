type: record
name: options
label: Celltypist specific options
fields:
  queryLayersKey:
    type: string?
    label: Data layer to use
    inputBinding:
      prefix: --query-layers-key
  ensembleLookup:
    type: File?
    inputBinding:
      prefix: --ensemble-lookup
